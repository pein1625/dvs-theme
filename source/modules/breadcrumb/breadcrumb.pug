mixin breadcrumb(...list)
  - var lastItem = list.pop()
  nav.m-breadcrumb
    .container
      ol.breadcrumb
        each item in list
          li.breadcrumb-item
            a(href="#!")= item
        li.breadcrumb-item.active= lastItem
